# set PATH so it includes user's private bin if it exists
[ -d "~/bin" -a -r "~/bin" ] && [[ ":$PATH:" != *":~/bin:"* ]] && export PATH="~/bin:$PATH"

# Also export /var/www/bin
[ -d "/var/www/bin" -a -r "/var/www/bin" ] && [[ ":$PATH:" != *":/var/www/bin:"* ]] && export PATH="$PATH:/var/www/bin"
